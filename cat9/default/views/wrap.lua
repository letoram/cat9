return
function(cat9, root, builtins, suggest, views)

function views.wrap(job, x, y, cols, rows, probe)
-- if selected, we can hint about scrollbar here
-- lash has a basic wrapping algorithm, use that
-- consume what we have
	return rows
end

end
